require 'spec_helper'

describe FlexibleOperators do
  it 'has a version number' do
    expect(FlexibleOperators::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
